apiVersion: v1

kind: Service

metadata:
  name: pagamentos-service

spec:
  selector:
    app: pagamentos-service

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3002

  type: ClusterIP